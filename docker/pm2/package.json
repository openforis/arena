{
  "name": "arena-pm2",
  "private": true,
  "description": "pm2 runtime for the Arena Docker image, with overrides for vulnerable pinned dependencies",
  "dependencies": {
    "pm2": "7.0.4"
  },
  "overrides": {
    "js-yaml": "^4.3.2"
  }
}
