/**
 * Canonical AI provider identifiers shared between client and server.
 * These are the string values stored in the database and exchanged over the API.
 */
export const AiProvider = {
  openai: 'openai',
  anthropic: 'anthropic',
  google: 'google',
  openaiCompatible: 'openai-compatible',
  vercelAiSdk: 'vercel-ai-sdk',
}
